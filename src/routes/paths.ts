export const BASE_PATH = '/contextium/v0';

export const BASE_PATHS = {
  config: '/config',
  documents: '/documents',
  search: '/search',
} as const;

export const CONFIG_PATHS = {
  ping: '/ping',
} as const;
